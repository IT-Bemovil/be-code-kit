# Sub-repositories (each has its own git — do NOT track here)
admin/
backend/
frontend/
bemovil2-proxy/
proxy/

# Node (root-level orchestration deps)
node_modules/

# Environment (secrets — NEVER track)
.env
.env.*

# autoSDD / Claude Code artifacts
.claude/skills/
.claude/settings.local.json
.claude/.stop-hook-fired
.claude/*.bak
.playwright-cli/
.claude/settings.json.bak

# IDE / AI tool configs (per-machine, not shared)
.vscode/
.cursor/
.windsurf/
.windsurfrules
.continue/
.agents/
.roo/
.trae/

# OS
.DS_Store
Thumbs.db

# Screenshots loose at root (should be in version folders)
*.png
*.jpg
*.webp

# Outputs (generated by agents, not source)
outputs/
